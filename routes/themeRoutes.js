// const express = require('express');
// const router = express.Router();
// const themeController = require('../controllers/themeController');
// const { ensureAuthenticated, ensureAdmin } = require('../middleware/auth');

// router.get('/admin/themes', ensureAuthenticated, ensureAdmin, themeController.listThemes);
// router.post('/admin/themes', ensureAuthenticated, ensureAdmin, themeController.addTheme);
// // Modifier un thème
// router.post('/admin/themes/:id/edit', ensureAuthenticated, ensureAdmin, themeController.editTheme);

// // Supprimer un thème
// router.post('/admin/themes/:id/delete', ensureAuthenticated, ensureAdmin, themeController.deleteTheme);

// module.exports = router;
